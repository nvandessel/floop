# bv (beads viewer) local config and caches
.bv/

# Built binaries
/floop

# Test coverage
coverage.out
coverage.html

# Audit logs (runtime data, not version controlled)
.floop/audit.jsonl
.floop/floop.db

# Beads runtime artifacts
.beads/export-state/
.beads/*.db-shm
.beads/*.db-wal
.beads/daemon.*

# Build artifacts
build/

# LanceDB native libraries (downloaded via scripts/download-artifacts.sh)
lib/
include/lancedb.h

# Git worktrees for parallel agent work
.worktrees/

# GoReleaser output
dist/

# Node.js (Playwright test dependencies)
node_modules/

# Gas Town (added by gt)
.runtime/
.claude/
.logs/
